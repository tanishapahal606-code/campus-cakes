services:
  # Option 1: Render Static Site (Highly Recommended - Free, Global CDN, Automatic)
  - type: web
    name: campus-cakes-static
    env: static
    buildCommand: npm run build
    publishDir: dist
    routes:
      - src: /*
        dest: /index.html
